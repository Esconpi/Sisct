@model Escon.SisctNET.Model.InternalAliquot

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="box box-primary">
    <div class="box-header">
        <a asp-action="Index" size="large" class="glyphicon glyphicon-circle-arrow-left pull-left"></a>
        <h3 class="box-title">
            <strong>Editar Aliquota</strong>
        </h3>

    </div>
    <hr />
    <div class="box-body">
        <div class="col-md-12">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="row">
                    <div class="form-group col-lg-8 col-md-6 col-xs-12">
                        <label asp-for="StateId" class="control-label"></label>
                        <select asp-items="@ViewBag.StateId" name="StateId" class="form-control selectoption" id="state"></select>
                        <span asp-validation-for="StateId" class="text-danger"></span>
                    </div>
                    <div class="form-group col-lg-2 col-md-3 col-xs-12">
                        <label asp-for="Aliquota" class="control-label"></label>
                        <input type="text" id="aliquota" name="aliquota" class="form-control money" value="@Model.Aliquota" required />
                    </div>

                    <div class="form-group col-lg-2 col-md-3 col-xs-12">
                        <label asp-for="DateStart" class="control-label"></label>
                        <input asp-for="DateStart" class="form-control" />
                        <span asp-validation-for="DateStart" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group col-lg-12 col-md-12 col-xs-12">
                    <input type="submit" value="Confirmar" class="btn btn-success pull-right" />
                </div>

            </form>
        </div>
    </div>
    <div class="box-footer">

    </div>
</div>

<script type="text/javascript">
    var state = @Model.StateId;
    $("#state").children('option').each(function () {
        if ($(this).val() == state) {
            $(this).attr('selected', true);
        }
    });
</script>
