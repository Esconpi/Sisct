<diV class="pull-right">
    <strong>Total Vendas: </strong> @string.Format("{0:C}", ViewBag.TotalVendas)
</diV>
<table class="table">
    <thead>
        <tr>
            <th>
                Ncm
            </th>
            <th>Descrição</th>
            <th width="15%">
                Valor
            </th>
            <th width="10%">
                Percentual %
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < ViewBag.Ncm.Count; i++)
        {
            <tr>
                <td>@ViewBag.Ncm[i][0]</td>
                <td>@ViewBag.Ncm[i][3]</td>
                <td style="text-align:right">@string.Format("{0:C}", Convert.ToDecimal(ViewBag.Ncm[i][1].Replace(',', '*').Replace('.', ',').Replace('*', '.')))</td>
                <td style="text-align:right">@string.Format("{0:C}", Convert.ToDecimal(ViewBag.Ncm[i][2].Replace(',', '*').Replace('.', ',').Replace('*', '.'))) %</td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <th>Total</th>
            <th></th>
            <td style="text-align:right">@string.Format("{0:C}", ViewBag.TotalNcm)</td>
            <td style="text-align:right">@string.Format("{0:N}", ViewBag.PercentualTotal) %</td>
        </tr>
    </tfoot>
</table>